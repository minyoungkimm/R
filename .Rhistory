cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
reple <- append(reple, unlist(reple_v))
cat(length(reple), "\n")
pageLink <- remDr$findElements(using='css','.review_ta.ng-scope > div.paginate > a.direction.next')
# clickelement가 잘 안먹힐때는 executeScript를 사용해 본다.
remDr$executeScript("arguments[0].click();",pageLink)
curr_PageElem <- remDr$findElement(using='css',' span > strong.ng-binding')
curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
curr_PageOldNum <- curr_PageNewNum;
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
reple <- append(reple, unlist(reple_v))
cat(length(reple), "\n")
pageLink <- remDr$findElements(using='css','.review_ta.ng-scope > div.paginate > a.direction.next')
# clickelement가 잘 안먹힐때는 executeScript를 사용해 본다.
remDr$executeScript("arguments[0].click();",pageLink)
curr_PageElem <- remDr$findElement(using='css',' span > strong.ng-binding')
curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
curr_PageElem <- remDr$findElement(using='css','div.paginate > span > strong.ng-binding')
curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
curr_PageElem <- remDr$findElement(using='css','div.paginate > span > strong.ng-binding')
curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
curr_PageNewNum <- curr_PageElem$getElementText()
cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
curr_PageElem <- remDr$findElement(using='css','body > div > div.ng-scope > div.container.ng-scope > div.content > div.hotel_used_review.ng-isolate-scope > div.review_ta.ng-scope > div.paginate > span:nth-child(4) > strong')
curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
curr_PageElem <- remDr$findElement(using='css','body > div > div.ng-scope > div.container.ng-scope > div.content > div.hotel_used_review.ng-isolate-scope > div.review_ta.ng-scope > div.paginate > span:ng-scope > strong.ng-binding')
curr_PageElem <- remDr$findElement(using='css','body > div > div.ng-scope > div.container.ng-scope > div.content > div.hotel_used_review.ng-isolate-scope > div.review_ta.ng-scope > div.paginate > span.ng-scope > strong.ng-binding')
curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
pageLink <- NULL
reple <- NULL
curr_PageOldNum <- 0
repeat{
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
Sys.sleep(1)
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
reple <- append(reple, unlist(reple_v))
cat(length(reple), "\n")
pageLink <- remDr$findElements(using='css','.review_ta.ng-scope > div.paginate > a.direction.next')
# clickelement가 잘 안먹힐때는 executeScript를 사용해 본다.
remDr$executeScript("arguments[0].click();",pageLink)
Sys.sleep(2)
curr_PageElem <- remDr$findElement(using='css',paste0('body > div > div.ng-scope > div.container.ng-scope > div.content > div.hotel_used_review.ng-isolate-scope > div.review_ta.ng-scope > div.paginate > span:nth-child(',curr_PageOldNum,') > strong'))
curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
#마지막 페이지를 확인하는 과정
if(curr_PageNewNum == curr_PageOldNum)  {
cat("종료\n")
break;
}
curr_PageOldNum <- curr_PageNewNum;
}
pageLink <- NULL
reple <- NULL
curr_PageOldNum <- 0
repeat{
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
Sys.sleep(1)
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
reple <- append(reple, unlist(reple_v))
cat(length(reple), "\n")
pageLink <- remDr$findElements(using='css','.review_ta.ng-scope > div.paginate > a.direction.next')
# clickelement가 잘 안먹힐때는 executeScript를 사용해 본다.
remDr$executeScript("arguments[0].click();",pageLink)
Sys.sleep(2)
curr_PageElem <- remDr$findElement(using='css',paste0("body > div > div.ng-scope > div.container.ng-scope > div.content > div.hotel_used_review.ng-isolate-scope > div.review_ta.ng-scope > div.paginate > span:nth-child(",curr_PageOldNum,") > strong"))
curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
#마지막 페이지를 확인하는 과정
if(curr_PageNewNum == curr_PageOldNum)  {
cat("종료\n")
break;
}
curr_PageOldNum <- curr_PageNewNum;
}
pageLink <- NULL
reple <- NULL
curr_PageOldNum <- 0
repeat{
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
Sys.sleep(1)
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
reple <- append(reple, unlist(reple_v))
cat(length(reple), "\n")
pageLink <- remDr$findElements(using='css','.review_ta.ng-scope > div.paginate > a.direction.next')
# clickelement가 잘 안먹힐때는 executeScript를 사용해 본다.
remDr$executeScript("arguments[0].click();",pageLink)
Sys.sleep(2)
curr_PageElem <- remDr$findElement(using='css',paste0("body > div > div.ng-scope > div.container.ng-scope > div.content > div.hotel_used_review.ng-isolate-scope > div.review_ta.ng-scope > div.paginate > span:nth-child(",curr_PageOldNum + 1,") > strong"))
curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
#마지막 페이지를 확인하는 과정
if(curr_PageNewNum == curr_PageOldNum)  {
cat("종료\n")
break;
}
curr_PageOldNum <- curr_PageNewNum;
}
pageLink <- NULL
reple <- NULL
curr_PageOldNum <- 0
repeat{
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
Sys.sleep(1)
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
reple <- append(reple, unlist(reple_v))
cat(length(reple), "\n")
pageLink <- remDr$findElements(using='css','.review_ta.ng-scope > div.paginate > a.direction.next')
# clickelement가 잘 안먹힐때는 executeScript를 사용해 본다.
remDr$executeScript("arguments[0].click();",pageLink)
Sys.sleep(2)
curr_PageElem <- remDr$findElement(using='css',paste0("body > div > div.ng-scope > div.container.ng-scope > div.content > div.hotel_used_review.ng-isolate-scope > div.review_ta.ng-scope > div.paginate > span:nth-child(",curr_PageOldNum + 2,") > strong"))
curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
#마지막 페이지를 확인하는 과정
if(curr_PageNewNum == curr_PageOldNum)  {
cat("종료\n")
break;
}
curr_PageOldNum <- curr_PageNewNum;
}
pageLink <- NULL
reple <- NULL
curr_PageOldNum <- 0
repeat{
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
Sys.sleep(1)
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
reple <- append(reple, unlist(reple_v))
cat(length(reple), "\n")
pageLink <- remDr$findElements(using='css','.review_ta.ng-scope > div.paginate > a.direction.next')
# clickelement가 잘 안먹힐때는 executeScript를 사용해 본다.
remDr$executeScript("arguments[0].click();",pageLink)
Sys.sleep(2)
curr_PageElem <- remDr$findElement(using='css',paste0("div.ng-scope > div.container.ng-scope > div.content > div.hotel_used_review.ng-isolate-scope > div.review_ta.ng-scope > div.paginate > span:nth-child(",curr_PageOldNum + 2,") > strong"))
curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
#마지막 페이지를 확인하는 과정
if(curr_PageNewNum == curr_PageOldNum)  {
cat("종료\n")
break;
}
curr_PageOldNum <- curr_PageNewNum;
}
pageLink <- NULL
reple <- NULL
curr_PageOldNum <- 0
repeat{
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
Sys.sleep(1)
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
reple <- append(reple, unlist(reple_v))
cat(length(reple), "\n")
pageLink <- remDr$findElements(using='css','.review_ta.ng-scope > div.paginate > a.direction.next')
# clickelement가 잘 안먹힐때는 executeScript를 사용해 본다.
remDr$executeScript("arguments[0].click();",pageLink)
Sys.sleep(2)
curr_PageElem <- remDr$findElement(using='css',paste0("span:nth-child(",curr_PageOldNum + 2,") > strong"))
curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
#마지막 페이지를 확인하는 과정
if(curr_PageNewNum == curr_PageOldNum)  {
cat("종료\n")
break;
}
curr_PageOldNum <- curr_PageNewNum;
}
curr_PageOldNum
curr_PageElem <- remDr$findElement(using='css',paste0("span:nth-child(",curr_PageOldNum + 2,") > strong"))
curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
pageLink <- NULL
reple <- NULL
curr_PageOldNum <- 0
repeat{
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
Sys.sleep(1)
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
reple <- append(reple, unlist(reple_v))
cat(length(reple), "\n")
Sys.sleep(2)
pageLink <- remDr$findElements(using='css','.review_ta.ng-scope > div.paginate > a.direction.next')
# clickelement가 잘 안먹힐때는 executeScript를 사용해 본다.
remDr$executeScript("arguments[0].click();",pageLink)
Sys.sleep(2)
curr_PageElem <- remDr$findElement(using='css',paste0("span > strong.ng-binding"))
curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
#마지막 페이지를 확인하는 과정
if(curr_PageNewNum == curr_PageOldNum)  {
cat("종료\n")
break;
}
curr_PageOldNum <- curr_PageNewNum;
}
pageLink <- NULL
reple <- NULL
curr_PageOldNum <- 0
repeat{
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
Sys.sleep(1)
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
reple <- append(reple, unlist(reple_v))
cat(length(reple), "\n")
Sys.sleep(2)
curr_PageElem <- remDr$findElement(using='css',paste0("span > strong.ng-binding"))
curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
#마지막 페이지를 확인하는 과정
if(curr_PageNewNum == curr_PageOldNum)  {
cat("종료\n")
break;
}
curr_PageOldNum <- curr_PageNewNum;
pageLink <- remDr$findElements(using='css','.review_ta.ng-scope > div.paginate > a.direction.next')
# clickelement가 잘 안먹힐때는 executeScript를 사용해 본다.
remDr$executeScript("arguments[0].click();",pageLink)
}
pageLink <- NULL
reple <- NULL
curr_PageOldNum <- 0
repeat{
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
Sys.sleep(1)
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
reple <- append(reple, unlist(reple_v))
cat(length(reple), "\n")
curr_PageElem <- remDr$findElement(using='css',paste0("span > .ng-binding"))
curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
#마지막 페이지를 확인하는 과정
if(curr_PageNewNum == curr_PageOldNum)  {
cat("종료\n")
break;
}
curr_PageOldNum <- curr_PageNewNum;
pageLink <- remDr$findElements(using='css','.review_ta.ng-scope > div.paginate > a.direction.next')
# clickelement가 잘 안먹힐때는 executeScript를 사용해 본다.
remDr$executeScript("arguments[0].click();",pageLink)
}
curr_PageElem$getElementText()
pageLink <- NULL
reple <- NULL
curr_PageOldNum <- 0
body > div > div.ng-scope > div.container.ng-scope > div.content > div.hotel_used_review.ng-isolate-scope > div.review_ta.ng-scope > div.paginate > span:nth-child(3) > strong
repeat{
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
Sys.sleep(1)
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
reple <- append(reple, unlist(reple_v))
cat(length(reple), "\n")
curr_PageElem <- remDr$findElement(using='css',paste0("span > a.ng-binding.ng-hide"))
curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
#마지막 페이지를 확인하는 과정
if(curr_PageNewNum == curr_PageOldNum)  {
cat("종료\n")
break;
}
curr_PageOldNum <- curr_PageNewNum;
pageLink <- remDr$findElements(using='css','.review_ta.ng-scope > div.paginate > a.direction.next')
# clickelement가 잘 안먹힐때는 executeScript를 사용해 본다.
remDr$executeScript("arguments[0].click();",pageLink)
}
pageLink <- NULL
reple <- NULL
curr_PageOldNum <- 0
repeat{
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
Sys.sleep(1)
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
reple <- append(reple, unlist(reple_v))
cat(length(reple), "\n")
curr_PageElem <- remDr$findElement(using='css',paste0("span > strong.ng-binding"))
curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
#마지막 페이지를 확인하는 과정
if(curr_PageNewNum == curr_PageOldNum)  {
cat("종료\n")
break;
}
curr_PageOldNum <- curr_PageNewNum;
pageLink <- remDr$findElements(using='css','.review_ta.ng-scope > div.paginate > a.direction.next')
# clickelement가 잘 안먹힐때는 executeScript를 사용해 본다.
remDr$executeScript("arguments[0].click();",pageLink)
}
pageLink <- NULL
reple <- NULL
curr_PageOldNum <- 0
repeat{
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
Sys.sleep(1)
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
reple <- append(reple, unlist(reple_v))
cat(length(reple), "\n")
curr_PageElem <- remDr$findElement(using='css',"span > strong.ng-binding")
curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
#마지막 페이지를 확인하는 과정
if(curr_PageNewNum == curr_PageOldNum)  {
cat("종료\n")
break;
}
curr_PageOldNum <- curr_PageNewNum;
pageLink <- remDr$findElements(using='css','.review_ta.ng-scope > div.paginate > a.direction.next')
# clickelement가 잘 안먹힐때는 executeScript를 사용해 본다.
remDr$executeScript("arguments[0].click();",pageLink)
}
remDr$navigate(url)
pageLink <- NULL
reple <- NULL
curr_PageOldNum <- 0
repeat{
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
Sys.sleep(1)
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
reple <- append(reple, unlist(reple_v))
cat(length(reple), "\n")
curr_PageElem <- remDr$findElement(using='css',"span > strong.ng-binding")
curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
if(curr_PageNewNum == curr_PageOldNum)  {
cat("종료\n")
break;
}
curr_PageOldNum <- curr_PageNewNum;
pageLink <- remDr$findElements(using='css','.review_ta.ng-scope > div.paginate > a.direction.next')
remDr$executeScript("arguments[0].click();",pageLink)
}
pageLink <- NULL
reple <- NULL
repeat{
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
Sys.sleep(1)
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
reple <- append(reple, unlist(reple_v))
cat(length(reple), "\n")
#curr_PageElem <- remDr$findElement(using='css',"span > strong.ng-binding")
#curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
#cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
#if(curr_PageNewNum == curr_PageOldNum)  {
#  cat("종료\n")
#  break;
#}
#curr_PageOldNum <- curr_PageNewNum;
pageLink <- remDr$findElements(using='css','.review_ta.ng-scope > div.paginate > a.direction.next')
remDr$executeScript("arguments[0].click();",pageLink)
}
pageLink <- NULL
reple <- NULL
curr_PageOldNum <- 0
repeat{
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
Sys.sleep(1)
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
un_v <- unlist(reple_v)
if(reple[length(reple)] == reple_v[length(reple_v)]){
break
}
reple <- append(reple, un_v)
cat(length(reple), "\n")
#curr_PageElem <- remDr$findElement(using='css',"span > strong.ng-binding")
#curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
#cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
#if(curr_PageNewNum == curr_PageOldNum)  {
#  cat("종료\n")
#  break;
#}
#curr_PageOldNum <- curr_PageNewNum;
pageLink <- remDr$findElements(using='css','.review_ta.ng-scope > div.paginate > a.direction.next')
remDr$executeScript("arguments[0].click();",pageLink)
}
pageLink <- NULL
reple <- 0
curr_PageOldNum <- 0
repeat{
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
Sys.sleep(1)
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
un_v <- unlist(reple_v)
if(reple[length(reple)] == reple_v[length(reple_v)]){
break
}
reple <- append(reple, un_v)
cat(length(reple), "\n")
#curr_PageElem <- remDr$findElement(using='css',"span > strong.ng-binding")
#curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
#cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
#if(curr_PageNewNum == curr_PageOldNum)  {
#  cat("종료\n")
#  break;
#}
#curr_PageOldNum <- curr_PageNewNum;
pageLink <- remDr$findElements(using='css','.review_ta.ng-scope > div.paginate > a.direction.next')
remDr$executeScript("arguments[0].click();",pageLink)
}
pageLink <- NULL
reple <-
curr_PageOldNum <- 0
body > div > div.ng-scope > div.container.ng-scope > div.content > div.hotel_used_review.ng-isolate-scope > div.review_ta.ng-scope > div.paginate > span:nth-child(3) > strong
repeat{
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
Sys.sleep(1)
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
un_v <- unlist(reple_v)
if(reple[length(reple)] == un_v[length(un_v)]){
break
}
reple <- append(reple, un_v)
cat(length(reple), "\n")
#curr_PageElem <- remDr$findElement(using='css',"span > strong.ng-binding")
#curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
#cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
#if(curr_PageNewNum == curr_PageOldNum)  {
#  cat("종료\n")
#  break;
#}
#curr_PageOldNum <- curr_PageNewNum;
pageLink <- remDr$findElements(using='css','.review_ta.ng-scope > div.paginate > a.direction.next')
remDr$executeScript("arguments[0].click();",pageLink)
}
cat(length(reple), "개의 댓글 추출\n")
pageLink <- NULL
reple <-
curr_PageOldNum <- 0
pageLink <- NULL
reple <- 0
curr_PageOldNum <- 0
repeat{
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
Sys.sleep(1)
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
un_v <- unlist(reple_v)
if(reple[length(reple)] == un_v[length(un_v)]){
break
}
reple <- append(reple, un_v)
cat(length(reple), "\n")
#curr_PageElem <- remDr$findElement(using='css',"span > strong.ng-binding")
#curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
#cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
#if(curr_PageNewNum == curr_PageOldNum)  {
#  cat("종료\n")
#  break;
#}
#curr_PageOldNum <- curr_PageNewNum;
pageLink <- remDr$findElements(using='css','.review_ta.ng-scope > div.paginate > a.direction.next')
remDr$executeScript("arguments[0].click();",pageLink)
}
pageLink <- NULL
reple <- 0
curr_PageOldNum <- 0
repeat{
doms <- remDr$findElements(using = "css selector", ".review_desc > p")
Sys.sleep(1)
reple_v <- sapply(doms, function (x) {x$getElementText()})
print(reple_v)
un_v <- unlist(reple_v)
if(reple[length(reple)] == un_v[length(un_v)]){
break
reple <- reple[-1]
}
reple <- append(reple, un_v)
cat(length(reple), "\n")
#curr_PageElem <- remDr$findElement(using='css',"span > strong.ng-binding")
#curr_PageNewNum <- as.numeric(curr_PageElem$getElementText())
#cat(paste(curr_PageOldNum, ':', curr_PageNewNum,'\n'))
#if(curr_PageNewNum == curr_PageOldNum)  {
#  cat("종료\n")
#  break;
#}
#curr_PageOldNum <- curr_PageNewNum;
pageLink <- remDr$findElements(using='css','.review_ta.ng-scope > div.paginate > a.direction.next')
remDr$executeScript("arguments[0].click();",pageLink)
}
cat(length(reple), "개의 댓글 추출\n")
reple
reple[-1]
cat(length(reple), "개의 댓글 추출\n")
cat(length(reple[-1]), "개의 댓글 추출\n")
write(reple[-1],"naverhotel.txt")
